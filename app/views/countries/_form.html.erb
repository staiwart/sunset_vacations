<%= form_with(model: country, local: true) do |form| %>
  <% if country.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(country.errors.count, "error") %> prohibited this country from being saved:</h2>

      <ul>
      <% country.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :country_name %>
  </div>

  <div class="field">
    <%= form.label :region %>
    <%= form.text_field :region, id: :country_region %>
  </div>

  <div class="field">
    <%= form.label :capital %>
    <%= form.text_field :capital, id: :country_capital %>
  </div>

  <div class="field">
    <%= form.label :language %>
    <%= form.text_field :language, id: :country_language %>
  </div>

  <div class="field">
    <%= form.label :currency %>
    <%= form.text_field :currency, id: :country_currency %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
